Flask==3.1.0
gunicorn==23.0.0
Pillow==11.0.0
numpy==2.1.3
# Used only when the host's Pillow is built without libraqm, so that Arabic
# text is still shaped and ordered correctly (see app/services/poster.py).
arabic-reshaper==3.0.0
python-bidi==0.6.6
firebase-admin==6.6.0
python-dotenv==1.0.1
# Reads the text layer of an uploaded fixtures PDF (see fixtures_parser).
pypdf==6.16.1
